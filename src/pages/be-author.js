export default function BeAuthorPage(props) {
  return <></>;
}

/**
 *
 * @param {import('next').GetServerSidePropsContext} ctx
 * @returns
 */
export async function getServerSideProps(ctx) {
  return {
    props: {},
  };
}
